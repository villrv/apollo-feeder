[project]
name = "apollo-feeder"
version = "0.1.0"
description = "The Apollo Feeder 9000"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apscheduler>=3.11.0",
    "flask>=3.1.0",
    "gunicorn>=23.0.0",
    "rpi-gpio>=0.7.1",
    "rpi-ws281x>=5.0.0",
]

[dependency-groups]
qr = [
    "dotenv>=0.9.9",
    "pillow>=11.1.0",
    "qrcode>=8.0",
]
